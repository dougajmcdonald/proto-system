{
  "name": "tenant-service",
  "version": "1.0.0",
  "description": "This repo is a mock tenant service, it's responsible for homing design system options (style properties) such as colour palettes, sizing scales etc",
  "main": "index.js",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "style-dictionary": "^2.10.2"
  },
  "scripts": {
    "start": "npm run clean && npm run build && node ./server.js",
    "build": "node ./build.js",
    "clean": "rm -rf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
